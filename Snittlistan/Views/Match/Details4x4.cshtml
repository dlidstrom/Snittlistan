@model Match4x4ViewModel
@{
    ViewBag.Title = string.Format("Match {0}-{1}", Model.HomeTeam.Name, Model.AwayTeam.Name);
}

<fieldset>
    <legend>Match</legend>
    @Html.DisplayFor(model => model.Match, "Match4x4Display")
    @if (Request.IsAuthenticated)
    {
        <p>@Html.ActionLink("Redigera", "EditDetails4x4", new { id = Model.Match.Id }, new { data_button = "data_button" })
        | @Html.ActionLink("Ta bort", "Delete4x4", new { id = Model.Match.Id }, new { data_button = "data_button" })</p>
    }
    <h3>Hemmalag</h3>
    @Html.DisplayFor(model => model.HomeTeam, "Team4x4Display")
    @if (Request.IsAuthenticated)
    {
        <p>@Html.ActionLink("Redigera", "EditTeam4x4", new { id = Model.Match.Id, isHomeTeam = true }, new { data_button = "data_button" })</p>
    }
    <h3>Bortalag</h3>
    @Html.DisplayFor(model => model.AwayTeam, "Team4x4Display")
    @if (Request.IsAuthenticated)
    {
        <p>@Html.ActionLink("Redigera", "EditTeam4x4", new { id = Model.Match.Id, isHomeTeam = false }, new { data_button = "data_button" })</p>
    }
</fieldset>