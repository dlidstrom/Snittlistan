@model RosterViewModel

@{
    ViewBag.Title = "Ta bort";
    Layout = "_Layout.cshtml";
}

<h2>Ta bort</h2>
<p>Är du säker på att du vill ta bort denna match?</p>
@using (Html.BeginForm("DeleteRoster", "App", FormMethod.Post))
{
    @Html.DisplayFor(model => model, "Roster")
    <input type="submit" value="Ta bort" />
    <a class="btn-link" href="@Url.Action("Index")">Avbryt</a>
}