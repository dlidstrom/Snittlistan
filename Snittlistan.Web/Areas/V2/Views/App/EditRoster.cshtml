@model EditRosterViewModel

@{
    ViewBag.Title = "Redigera match";
}

<div class="row">
    <div class="span6">
        <h3>@ViewBag.Title</h3>
        @using (Html.BeginForm("EditRoster", "App", FormMethod.Post, new { @class = "well form-horizontal"}))
        {
            if (Html.ViewData.ModelState.IsValid == false)
            {
                @Html.ValidationSummary(false, "Det gick inte att spara. Försök gärna igen.")
            }
            @Html.EditorFor(model => model, "RosterEditor")
            <div class="control-group">
                <div class="controls">
                    <button class="btn btn-primary" type="submit">Spara</button>
                </div>
            </div>
        }
    </div>
</div>
