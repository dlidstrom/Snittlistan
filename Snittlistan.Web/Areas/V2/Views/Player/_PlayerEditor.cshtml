@using Snittlistan.Web.Areas.V2.Domain
@model CreatePlayerViewModel

<div class="control-group">
    <label class="control-label" for="name">Namn:</label>
    <div class="controls">
        <input type="text"
               name="name"
               placeholder="Fullständigt namn"
               autofocus="autofocus"
               autocorrect="off"
               autocomplete="off"
               value="@Model.Name"
               required />
    </div>
</div>
<div class="control-group">
    <label class="control-label" for="email">E-postadress:</label>
    <div class="controls">
        <input type="email"
               name="email"
               placeholder=""
               autocorrect="off"
               autocomplete="off"
               value="@Model.Email"
               required />
    </div>
</div>
<div class="control-group">
    <label class="control-label" for="status">Status:</label>
    <div class="controls">
        <select name="status">
            <option value="Active"
                @if (Model.Status == Player.Status.Active)
                {
                    <text>selected="selected"</text>
                }
                >
                Aktiv
            </option>
            <option value="Supporter"
                @if (Model.Status == Player.Status.Supporter)
                {
                    <text>selected="selected"</text>
                }
                >
                Supporter
            </option>
            <option value="Inactive"
                @if (Model.Status == Player.Status.Inactive)
                {
                    <text>selected="selected"</text>
                }
                >
                Inaktiv
            </option>
        </select>
    </div>
</div>
